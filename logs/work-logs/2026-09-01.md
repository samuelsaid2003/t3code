# T3 Code work log — 1 September 2026

## 07:01 — Paused Slack message-provenance implementation

- **Outcome:** Partially added durable Slack provenance to orchestration
  messages. Draft changes introduce a Slack external-source field, delivery
  receipts, a delivery-record command/event, projection persistence and
  migration `47`. The local bridge now attempts to mark an assistant message as
  delivered only after its Slack post succeeds, including routine responses
  when an assistant message can be identified.
- **Boundary:** The requested web message markers are not implemented. The
  intended UI remains a Slack mark with `Received from Slack` on inbound user
  messages and a Slack mark plus forward indicator with `Delivered to Slack` on
  confirmed outbound assistant messages.
- **Verification:** No formatting, typecheck, focused test, migration test, or
  client acceptance pass has run since these draft changes began. Existing
  bridge tests still assert the pre-change `ask()` return shape and tracker
  methods, so the current worktree must be treated as unverified. `git diff
--check` reports no whitespace errors only.
- **Prior green baseline:** Before this follow-up, the base local bridge passed
  14 tests across six files, the bridge and client-runtime typechecks,
  formatting, and `git diff --check`.
- **Hosted actions:** None. No Slack app or token, T3 pairing, live message,
  LaunchAgent, commit, push, deployment, or release was created. The bridge
  remains offline and the earlier hosted-agent scaffold remains deleted.
- **Follow-up:** Update contract/decider/reducer/projection and bridge tests,
  implement the web markers and tooltips, run focused verification, then perform
  a foreground same-thread Slack DM smoke test only after explicit live-setup
  authorisation.

## 09:03 — Slack provenance and selective delivery completed

- **Outcome:** Completed migration `47` and the message-origin/delivery-receipt
  path across contracts, orchestration, persistence, snapshots and client
  reducers. Desktop and mobile timelines now show accessible Slack provenance
  markers with the exact labels `Received from Slack` and `Delivered to Slack`.
  The local bridge records delivery only after every Slack chunk posts and
  retries transient T3 receipt failures twice.
- **Decisions:** Selective delivery remains strict: Slack-originated turns and
  scheduled routine completion/failure/attention notifications go to Slack;
  ordinary T3-originated messages do not. Delivery is practical at-least-once,
  with the rare post-before-receipt crash duplicate documented. Live pairing
  instructions now target Samuel's isolated `.t3-samuel` home rather than the
  Alpha data directory.
- **Verification:** Ten focused test files passed with 57 tests across the
  bridge, decider, migration and client reducer, including an explicit ordinary
  T3-message non-forwarding test. A wider UI-focused run passed 93 tests. Bridge,
  server, web and mobile typechecks passed; the server retained unrelated
  pre-existing Effect suggestions only. `git diff --check` passed.
- **Hosted actions:** No Slack credentials, pairing, live message, LaunchAgent,
  push, deployment or release was created. Foreground live Slack acceptance
  remains gated on explicit setup permission and credentials.
- **Repository state:** The logging policy was committed separately as
  `2198daa9f`; the Slack feature is ready for its checkpoint commit before the
  upstream merge.

## 09:17 — Upstream `v0.0.37` integration

- **Outcome:** Integrated the final upstream `v0.0.37` release tag after the
  Slack checkpoint. Resolved nine conflicts while preserving Samuel's desktop
  and mobile identities, Agent Chats, side chat, multi-group split view,
  compact composer, historical-ref file previews, and Slack provenance. The
  merge also retains upstream's mobile attachment/voice work, Markdown preview,
  video attachment handling and performance changes.
- **Recovery:** The first dependency refresh ran inside the network sandbox and
  stalled on registry DNS after recreating `node_modules`. It was stopped and
  rerun with approved registry access against the already-merged frozen
  lockfile; the 17-package workspace then installed successfully. One semantic
  file-preview import missed by textual conflict resolution was found by the
  web typecheck and repaired before tests.
- **Verification:** Desktop, server, web, mobile and Slack-bridge typechecks
  passed; server/desktop retained upstream Effect suggestions only. Nineteen
  focused test files passed with 277 tests covering the conflict areas, side
  chat, split groups, composer behavior, Slack and migration `47`.
- **Data safety:** A consistent read-only snapshot of Samuel's live database was
  migrated from `46` to `47`. Integrity remained `ok`, and counts remained 186
  threads, 14,558 messages, 295,454 events and 185 provider-session rows. The
  live database remained on `46` and was never written; the 3.1 GB temporary
  snapshot was deleted after verification.
- **Hosted actions:** Fetched from read-only `upstream`; no origin push, tag or
  release was created.
