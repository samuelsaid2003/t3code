# T3 Code work log — 25 August 2026

> Historical reconstruction from the fork's first-parent Git history, release tags, GitHub release workflow records, changed files, and durable repository docs. It is not a transcript, and it begins at the first Samuel-specific commit after upstream base `99960383d`.

## Desktop and usage foundations

- Added independent desktop app windows, including window-aware navigation, menus, IPC, preview handling, and persistence (`943b590c5`).
- Added Codex subscription usage and remaining-allowance display, then generalized the sidebar path for Claude allowance data (`3c4ba9a38`, `1a2317610`, `8135368d1`, `d1bafa5a3`).
- Kept Chromium developer tools opt-in during development after the desktop changes exposed an unwanted automatic path (`6668f4f2f`).

## Isolated Samuel desktop distribution

- Established the fork as a separately installable macOS app with its own name, bundle identity, URL scheme, user-data and T3 home locations, ports, signing identity, update feed, packaging workflow, release guard, and distribution tests (`f1457a3e6`). This created the isolation boundary that lets T3 Code (Samuel) coexist with T3 Code Alpha.
- Added a one-way Alpha state importer using a consistent SQLite snapshot and validation instead of pointing the fork at Alpha's live database (`fca81f62d`).
- Made passkeys optional for the fork's release path so the independent distribution did not depend on upstream hosted passkey infrastructure (`b99b803d3`).
- Recovered several launch and release edge cases: excluded the desktop debug manifest from packages (`db8beb14c`), preserved imported provider resume cursors without carrying transient active runtime state (`ce43507b4`), and allowed the Samuel renderer origin at the server boundary (`faca1b1c9`).

## Provider allowance windows

- Extended Claude usage from a single remaining value to multiple named allowance windows and updated the contracts, server driver, provider snapshots, sidebar rendering, tests, and user documentation (`d8fd56410`).

## Multi-pane thread workspace

- Added the first multi-pane thread workspace with draggable panes and durable layout state (`3ad545225`).
- Stabilized inactive pane headers so activity in another pane did not disturb their presentation (`633b13ad2`).
- Added the Codex usage meter to the generalized sidebar usage UI (`a93421ae8`).
- Preserved one split thread group across navigation and reloads as the first persistence slice for split view (`e7673a71c`).

## Releases

GitHub records successful macOS fork release workflows for every release published that day:

| Release  | Commit      | Included milestone                                        |
| -------- | ----------- | --------------------------------------------------------- |
| `v0.1.0` | `b99b803d3` | Isolated fork, safe Alpha import, optional fork passkeys  |
| `v0.1.1` | `d8fd56410` | Imported cursor preservation and Claude allowance windows |
| `v0.1.2` | `3ad545225` | Samuel renderer-origin fix and multi-pane workspace       |
| `v0.1.3` | `a93421ae8` | Stable pane headers and Codex usage meter                 |
| `v0.1.4` | `e7673a71c` | Persisted split thread group                              |

- **Verification boundary:** the historical record proves committed test coverage was added with the relevant features and that all five GitHub release workflows completed successfully. It does not preserve the details of any manual desktop sessions.
