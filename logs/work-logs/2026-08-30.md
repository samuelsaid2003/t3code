# T3 Code work log — 30 August 2026

> Historical reconstruction from first-parent Git history, release tags, GitHub workflow records, focused tests in the commits, durable docs, and the completed development-session record. It is not a raw transcript.

## Codex side chat

- Added one temporary Codex side chat per parent thread as a normal desktop right-panel tab (`1f1a1a66b`). Side chat forks the parent's persisted Codex provider thread, injects the side-conversation boundary, inherits the parent's workspace and runtime configuration, and keeps parent and child turns independent after the fork snapshot.
- Added the durable parent/child relation and side-thread kind across contracts, decider, projector, migration `46`, provider service/reactor, and Codex runtime. Fork or boundary-injection failures surface as errors and never silently fall back to a blank conversation.
- Added desktop launcher, loading/error/retry states, side-panel presentation for the existing chat timeline and composer, close-to-delete lifecycle, and filtering so web/mobile/general navigation decode but do not expose side threads.
- Focused contract, server, provider-runtime, right-panel, and UI tests accompanied the implementation. A development desktop acceptance pass exercised the feature end to end before release.

## Reliable multiple split-view groups

- Reworked split workspace persistence so multiple independent split-view groups survive switching, reload, insertion, removal, and reconciliation (`fb94f8d27`). This replaced the earlier one-preserved-group limitation and added extensive store and sidebar regression coverage.

## Compact desktop composer

- Moved checkout and branch controls into the draggable desktop thread header, removed the duplicate run-context strip below the composer, lowered the composer toward the viewport edge, and reduced its default height (`a6e0b944d`).
- After visual review, restored one line of default height and raised the composer slightly. The resulting desktop composer starts at two lines and still expands for multiline input; existing web and mobile context layouts remain intact.
- Updated focused composer/header tests and user documentation with the layout change.

## Releases

- Published `v0.1.12` from `fb94f8d27`, containing the pending 29 August nightly/mobile work, Codex side chat, and reliable multiple split-view groups. GitHub Actions run [`33303277072`](https://github.com/samuelsaid2003/t3code/actions/runs/33303277072) completed successfully.
- Published `v0.1.13` from `a6e0b944d`, adding the compact composer layout. GitHub Actions run [`33307668025`](https://github.com/samuelsaid2003/t3code/actions/runs/33307668025) completed successfully; the release is marked latest.
- Both release workflows built the fork's signed/notarized macOS artifacts for both supported architectures and published the merged update manifest through the dedicated Samuel workflow.
- **State:** all three milestones were committed to `main`, tagged, pushed to `origin`, and released. The development app used for acceptance was closed before the final release cycle.
