# T3 Code work log — 29 August 2026

> Historical reconstruction from first-parent Git history, release tags, GitHub workflow records, changed files, migrations, tests, and durable docs. It is not a transcript.

## Persistent Agent Chats and routines

- Added durable Agent Chats and scheduled routines as first-class thread behavior (`ecd1ebe96`). The slice spans contracts, the event-sourced decider and projector, migrations `44` and `45`, provider/runtime projections, a routine reactor and schedule logic, desktop/web navigation, profile and notification UI, tests, and user/internal documentation.
- The durable model kept agent conversations in normal server-owned history instead of treating them as client-only shortcuts.

## Upstream `v0.0.36` integration and release

- Merged upstream `v0.0.36` into the fork (`d46dbfcd4`). The large change set is recorded as upstream integration; the fork work was resolving it against Samuel's distribution, provider, history, and workspace customizations.
- Published `v0.1.11` from `d46dbfcd4`, which included persistent Agent Chats and routines. GitHub Actions run [`33237334754`](https://github.com/samuelsaid2003/t3code/actions/runs/33237334754) completed successfully.

## Nightly upstream integration

- Merged the upstream `v0.0.37` nightly (`67b5a98c3`). Among the imported upstream areas were environment theming, generalized attachments, and project/settings updates. These are logged as integrated upstream capabilities, not fork-authored features.

## Mobile Agent Chats

- Added first-class Agent Chats to the React Native client (`aab1e9602`), including the Agent Chats list, mode controls, navigation and settings integration, mobile composer persistence, tests, and user documentation.
- Added Samuel-specific iOS release configuration and workflow changes alongside the mobile feature.
- **Release boundary:** `v0.1.11` ended at `d46dbfcd4`. The later nightly integration and mobile Agent Chats remained committed on `main` and were first included in the next desktop release, `v0.1.12`, on 30 August.
